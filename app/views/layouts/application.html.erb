<!DOCTYPE html>
<html>
<head>
  <title>Shop</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <link href='https://fonts.googleapis.com/css?family=Pacifico' rel='stylesheet' type='text/css'>
      <link href='https://fonts.googleapis.com/css?family=Josefin+Sans:400,100,300,600,700' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="top-nav">
    <div class="col-xs-10 col-xs-offset-1">
      <div class="brand pull-left">
        <%= link_to 'Shop', root_path %>
      </div>
      <div id:"navmenulist">
          <%= form_tag(products_path, :method => "get", class: "navbar-form", id: "search-form") do %>
          <div class="input-append text-center">
            <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search the Store" %>
            <button type="button" class="btn btn-default btn-med">Search</button>
          <% end %>
        &nbsp;&nbsp;
       <% if current_admin %>
       Admin
        <%= link_to "New Post", new_admin_product_path %>&nbsp;&nbsp;
        <%= link_to "Sign out", destroy_admin_session_path, method: :delete %>&nbsp;
        &nbsp;
          <% else %>
        <%= link_to "Sign In", new_admin_session_path %> &nbsp;&nbsp;
      <% end %>
      </div>
      </div>
    </div>
  </div>
  <div class="row">
   <div class='category_menu col-xs-10 col-xs-offset-1'>
   <hr>
   <ul>
    <% categories.each do |category| %>
   <li><%= link_to category.kind, category_products_path(category) %></li>&nbsp;&nbsp;
   <% end %>
   </ul>
   <hr>
   </div>
   </div>

  <% if notice.present? %>
    <p class="notice"><%= notice %></p>
  <% end %>
  
  <% if alert.present? %>
    <p class="alert"><%= alert %></p>
  <% end %>


<%= yield %>

</body>
</html>
